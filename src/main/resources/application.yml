spring:  
  datasource:  
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/db_test  
    username: root  
    password: 000000
  jpa:
    hibernate:
     ddl-auto: update #ddl-auto:设为create表示每次都重新建表,修改配置文件中的ddl-auto为update,否则每次启动都会重建表:
    show-sql: true
mybatis:
    mapper-locations: classpath:mapper/*.xml
    typeAliasesPackage: com.e1858.building.models